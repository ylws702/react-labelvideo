const host = "//localhost:3333";
// const host = ".";

const urls = {
  hello: `${host}/api/hello`,
  files: `${host}/api/files`,
  video: `${host}/api/video`,
  label: `${host}/api/label`,
  videoLabel: `${host}/api/videoLabel`,
};

export default urls;
